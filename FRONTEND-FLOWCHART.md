# 🔄 فلوچارت دقیق Frontend: مدیریت Session و جستجو

> **نمودار کامل فرایند برای Frontend Developer**

---

## 📊 فلوچارت اصلی (Main Flow)

```
┌─────────────────────────────────────────────────────────────────┐
│                     START: App Mount                             │
│                  (کاربر وارد اپلیکیشن می‌شود)                   │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                                 ▼
                    ┌────────────────────────┐
                    │  شناسایی کاربر (userId) │
                    │  از Authentication      │
                    │  System دریافت می‌شود   │
                    └────────────┬───────────┘
                                 │
                                 ▼
                ┌────────────────────────────────────┐
                │  چک کردن Session در Local Storage │
                │  sessionData = localStorage.get()   │
                └────────────┬───────────────────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
        [Session وجود دارد؟]      [Session وجود ندارد]
                │                         │
                │                         │
        ┌───────▼─────────┐              │
        │  بله، وجود دارد  │              │
        └───────┬─────────┘              │
                │                         │
                ▼                         │
┌───────────────────────────────┐        │
│  Validate Session با API      │        │
│  GET /api/health/session      │        │
│  { sessionId: "xxx" }         │        │
└───────┬───────────────────────┘        │
        │                                 │
        ▼                                 │
[Session معتبر است؟]                     │
        │                                 │
   ┌────┴────┐                           │
   │         │                           │
  بله       خیر                          │
   │         │                           │
   │         └──────────┐                │
   │                    │                │
   │                    ▼                │
   │     ┌──────────────────────────┐   │
   │     │ حذف Session نامعتبر      │   │
   │     │ localStorage.remove()     │   │
   │     └──────────────┬───────────┘   │
   │                    │                │
   │                    └────────────────┘
   │                                     │
   │    ┌────────────────────────────────┘
   │    │
   │    ▼
   │  ┌─────────────────────────────────┐
   │  │  ایجاد Session جدید             │
   │  │  POST /api/session              │
   │  │  { userId: "user-123" }         │
   │  └──────────────┬──────────────────┘
   │                 │
   │                 ▼
   │       ┌─────────────────────┐
   │       │  دریافت sessionId   │
   │       │  { sessionId: "xxx" }│
   │       └─────────┬───────────┘
   │                 │
   │                 ▼
   │    ┌──────────────────────────┐
   │    │  ذخیره در Local Storage  │
   │    │  localStorage.set()       │
   │    └──────────┬───────────────┘
   │               │
   └───────────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │  App آماده است!      │
        │  isReady = true       │
        └──────────┬───────────┘
                   │
                   ▼
        ┌──────────────────────────────┐
        │  نمایش Search Interface      │
        │  (کاربر می‌تواند جستجو کند)  │
        └──────────┬───────────────────┘
                   │
                   ▼
        ╔══════════════════════════════╗
        ║    MAIN LOOP: User Actions   ║
        ║    (تا زمان بستن اپلیکیشن)   ║
        ╚══════════╤═══════════════════╝
                   │
      ┌────────────┼────────────┐
      │            │            │
      ▼            ▼            ▼
  [Action 1]  [Action 2]  [Action 3]
  جستجوی جدید  صفحه بعدی   دیدن تاریخچه
      │            │            │
      │            │            │
      └────────────┴────────────┘
                   │
                   ▼
            [ادامه فرایند...]


┌─────────────────────────────────────────────────────────────────┐
│                   END: App Unmount                               │
│                   (کاربر از اپلیکیشن خارج می‌شود)               │
│                                                                   │
│                   ▼                                               │
│          ┌────────────────────┐                                  │
│          │  Cleanup (اختیاری) │                                  │
│          │  DELETE /api/session│                                 │
│          └────────────────────┘                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔍 فلوچارت دقیق: Action 1 - جستجوی جدید

```
کاربر کلیدواژه و فیلترها را وارد می‌کند
              ↓
    [دکمه Search کلیک می‌شود]
              ↓
      ┌───────────────┐
      │ Validation    │
      │ keywords? ✓   │
      │ sessionId? ✓  │
      └───────┬───────┘
              │
              ▼
      ┌───────────────────┐
      │  setLoading(true) │
      │  setError(null)   │
      └───────┬───────────┘
              │
              ▼
      ┌─────────────────────────────┐
      │  POST /api/search           │
      │  {                          │
      │    userId: "user-123",      │
      │    sessionId: "xxx",        │
      │    keywords: "AI",          │
      │    filters: {...},          │
      │    page: 1                  │
      │  }                          │
      └───────┬─────────────────────┘
              │
              ▼
        [API Response]
              │
      ┌───────┴────────┐
      │                │
      ▼                ▼
  [موفق]          [خطا]
      │                │
      │                ▼
      │    ┌───────────────────────┐
      │    │  مدیریت خطا            │
      │    │                        │
      │    │  Session منقضی؟       │
      │    │    → ایجاد Session جدید│
      │    │                        │
      │    │  503 - No Tabs?       │
      │    │    → نمایش Queue       │
      │    │    → Retry بعد 5 ثانیه│
      │    │                        │
      │    │  Network Error?       │
      │    │    → نمایش خطا         │
      │    │    → دکمه Retry        │
      │    └───────────────────────┘
      │
      ▼
┌──────────────────────┐
│  دریافت نتایج        │
│  {                   │
│    searchId: "yyy",  │
│    results: [...],   │
│    currentPage: 1,   │
│    totalPages: 15    │
│  }                   │
└──────┬───────────────┘
       │
       ▼
┌─────────────────────┐
│  ذخیره در State     │
│  setCurrentSearch()  │
│  setLoading(false)   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  نمایش نتایج        │
│  - ResultsList      │
│  - Pagination       │
│  - Load More btn    │
└─────────────────────┘
```

---

## 📄 فلوچارت دقیق: Action 2 - صفحه بعدی

```
کاربر دکمه "Load More" یا صفحه بعدی را می‌زند
              ↓
      ┌───────────────┐
      │ چک کردن:      │
      │ searchId? ✓   │
      │ sessionId? ✓  │
      │ hasNextPage? ✓│
      └───────┬───────┘
              │
              ▼
      ┌─────────────────────────────┐
      │  setLoading(true)           │
      └───────┬─────────────────────┘
              │
              ▼
      ┌─────────────────────────────────┐
      │  POST /api/search/:id/continue  │
      │  {                              │
      │    sessionId: "xxx",            │
      │    page: 2                      │
      │  }                              │
      └───────┬─────────────────────────┘
              │
              ▼
        [API Response]
              │
      ┌───────┴────────┐
      │                │
      ▼                ▼
  [موفق]          [خطا]
      │                │
      │                ▼
      │         [مدیریت خطا]
      │
      ▼
┌──────────────────────┐
│  دریافت نتایج جدید   │
│  {                   │
│    results: [...],   │
│    currentPage: 2,   │
│    totalPages: 15    │
│  }                   │
└──────┬───────────────┘
       │
       ▼
┌─────────────────────────────┐
│  به‌روزرسانی State           │
│  setCurrentSearch(prev => ({ │
│    ...prev,                  │
│    currentPage: 2,           │
│    results: [                │
│      ...prev.results,        │
│      ...newResults           │
│    ]                         │
│  }))                         │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────┐
│  Scroll به نتایج جدید│
│  (اختیاری)           │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  نمایش نتایج        │
│  setLoading(false)   │
└─────────────────────┘
```

---

## 🗂️ فلوچارت دقیق: Action 3 - مشاهده تاریخچه

```
کاربر روی "History" کلیک می‌کند
              ↓
      ┌───────────────┐
      │ چک کردن:      │
      │ sessionId? ✓  │
      └───────┬───────┘
              │
              ▼
      ┌─────────────────────────────┐
      │  GET /api/search/history/xxx│
      └───────┬─────────────────────┘
              │
              ▼
        [API Response]
              │
              ▼
┌──────────────────────────────┐
│  دریافت لیست جستجوهای قبلی │
│  [                           │
│    {                         │
│      id: "search-1",         │
│      query: "AI",            │
│      currentPage: 3,         │
│      totalPages: 10,         │
│      createdAt: "..."        │
│    },                        │
│    ...                       │
│  ]                           │
└──────┬───────────────────────┘
       │
       ▼
┌─────────────────────┐
│  نمایش لیست          │
│  - هر جستجو          │
│  - تاریخ و ساعت      │
│  - دکمه "Rerun"      │
│  - دکمه "Continue"   │
└──────┬──────────────┘
       │
       ▼
[کاربر می‌تواند جستجوی قبلی را از سر بگیرد]
```

---

## 🔄 فلوچارت: Session Lifecycle

```
┌─────────────────────────────────────────────────────────┐
│                  Session Lifecycle                       │
└─────────────────────────────────────────────────────────┘

   App Mount                Session Check            Session Valid
       │                          │                        │
       │                          │                        │
       ▼                          ▼                        ▼
┌────────────┐           ┌────────────────┐      ┌─────────────┐
│ Check      │           │ Validate with  │      │ Use Existing│
│ localStorage│──── YES ─→│ API            │─ YES ─→│ Session     │
│            │           │                │      │             │
└────┬───────┘           └────────┬───────┘      └─────────────┘
     │                            │
     NO                          NO/ERROR
     │                            │
     └────────────────┬───────────┘
                      │
                      ▼
           ┌─────────────────────┐
           │ Create New Session  │
           │ POST /api/session   │
           └──────────┬──────────┘
                      │
                      ▼
           ┌─────────────────────┐
           │ Save to localStorage│
           │ sessionData = {     │
           │   sessionId,        │
           │   userId,           │
           │   createdAt,        │
           │   expiresAt         │
           │ }                   │
           └──────────┬──────────┘
                      │
                      ▼
              ┌───────────────┐
              │  Ready to Use │
              └───────────────┘


┌─────────────────────────────────────────────────────────┐
│              Session در زمان‌های مختلف                  │
└─────────────────────────────────────────────────────────┘

Timeline:

T=0 (App Mount)
├─ Check localStorage
├─ Found? → Validate
└─ Not Found? → Create New

T=5 minutes (User Idle)
├─ Session: ✓ Still Alive (24h lifetime)
├─ Tab: ✗ Released (10min timeout)
└─ Cache: ✓ Still Valid (15min TTL)

T=20 minutes (User Returns)
├─ Session: ✓ Still Alive
├─ Tab: ✗ Released (will acquire new one)
├─ Cache: ✗ Expired (will refetch)
└─ User can continue from last page ✓

T=24 hours (Session Expires)
├─ Session: ✗ Expired
└─ Must create new session

T=App Unmount
└─ Optional: DELETE /api/session/:id
```

---

## 🔐 فلوچارت: Session Storage Strategy

```
┌─────────────────────────────────────────────────────────┐
│            چگونه Session را ذخیره کنیم؟                 │
└─────────────────────────────────────────────────────────┘

Option 1: localStorage (Persistent)
─────────────────────────────────────
✅ مزایا:
   - بعد از بستن و باز کردن مرورگر باقی می‌ماند
   - کاربر می‌تواند از جایی که بوده ادامه دهد
   - مناسب برای Desktop Apps

❌ معایب:
   - در Incognito/Private Mode کار نمی‌کند
   - محدودیت 5-10MB
   - باید Expiration را خودمان چک کنیم

🎯 کاربرد:
   → وقتی می‌خواهید کاربر بعد از بستن مرورگر
     هم بتواند ادامه دهد


Option 2: sessionStorage (Tab-based)
─────────────────────────────────────
✅ مزایا:
   - جدا برای هر Tab
   - بعد از بستن Tab پاک می‌شود (Auto cleanup)
   - امنیت بیشتر

❌ معایب:
   - بعد از بستن Tab از بین می‌رود
   - Duplicate Tab → جلسه جدید

🎯 کاربرد:
   → وقتی می‌خواهید هر Tab جلسه مستقل داشته باشد


Option 3: Memory (State Only)
──────────────────────────────
✅ مزایا:
   - سریع‌ترین
   - بدون نگرانی Storage

❌ معایب:
   - با Refresh صفحه از بین می‌رود
   - نیاز به Session جدید با هر Refresh

🎯 کاربرد:
   → Prototypes یا SPA های ساده


Option 4: Cookie (Recommended for Production)
──────────────────────────────────────────────
✅ مزایا:
   - Auto-send با هر request
   - Expiration داخلی
   - Secure, HttpOnly options
   - کار می‌کند با SSR

❌ معایب:
   - محدودیت 4KB
   - پیچیده‌تر برای setup

🎯 کاربرد:
   → Production apps با Authentication


┌─────────────────────────────────────────────────────────┐
│                  پیشنهاد ما:                            │
│                                                          │
│  Development: localStorage                              │
│  Production: Cookie + localStorage (fallback)           │
└─────────────────────────────────────────────────────────┘
```

---

## ⚠️ فلوچارت: Error Handling

```
                    [API Call شروع می‌شود]
                              │
                              ▼
                        [Request Send]
                              │
                              │
                ┌─────────────┴─────────────┐
                │                           │
                ▼                           ▼
          [Success 200]              [Error Occurred]
                │                           │
                │                           │
                │               ┌───────────┴───────────┐
                │               │                       │
                │               ▼                       ▼
                │         [Network Error]         [API Error]
                │               │                       │
                │               │                       │
                │               ▼                       ▼
                │    ┌──────────────────┐    ┌─────────────────┐
                │    │ خطای شبکه         │    │ Response Status │
                │    │                  │    │                 │
                │    │ ├─ Timeout       │    │ ├─ 400 Bad Req  │
                │    │ ├─ No Internet   │    │ ├─ 404 Not Found│
                │    │ └─ DNS Fail      │    │ ├─ 429 Rate Lim │
                │    │                  │    │ ├─ 500 Server   │
                │    │ Action:          │    │ └─ 503 No Tabs  │
                │    │ → Show Retry Btn │    │                 │
                │    │ → Check Internet │    │ Action:         │
                │    └──────────────────┘    │ → Parse error   │
                │                            │ → Show message  │
                │                            │ → Specific fix  │
                │                            └─────────────────┘
                │
                ▼
      ┌──────────────────┐
      │  Process Data    │
      │  Success!        │
      └──────────────────┘


┌─────────────────────────────────────────────────────────────┐
│              خطاهای خاص و راه حل                             │
└─────────────────────────────────────────────────────────────┘

Error 1: "Session not found" (404)
───────────────────────────────────
سبب: Session منقضی شده یا حذف شده

راه حل:
1. حذف sessionId از localStorage
2. ایجاد Session جدید
3. Retry request با Session جدید
4. نمایش Toast: "Session renewed"

Code:
if (error.includes('Session not found')) {
  localStorage.removeItem('sessionId');
  const newSession = await createSession(userId);
  return retry(newSession.sessionId);
}


Error 2: "Service temporarily unavailable" (503)
─────────────────────────────────────────────────
سبب: تمام Browser Tabs پر هستند

راه حل:
1. نمایش پیام: "Server is busy, you're in queue"
2. نمایش تخمین زمان: "~30 seconds"
3. Auto-retry بعد 5-10 ثانیه
4. حداکثر 3 بار تلاش

Code:
if (error.status === 503) {
  showToast('Server is busy, retrying...');
  await sleep(5000);
  return retry(maxRetries - 1);
}


Error 3: "Rate limit exceeded" (429)
─────────────────────────────────────
سبب: تعداد درخواست بیش از حد

راه حل:
1. نمایش پیام: "Too many requests"
2. خواندن header: Retry-After
3. Disable دکمه Search
4. نمایش countdown timer

Code:
if (error.status === 429) {
  const retryAfter = error.headers['retry-after'];
  showCountdown(retryAfter);
  setTimeout(() => enableSearch(), retryAfter * 1000);
}


Error 4: Network Timeout
─────────────────────────
سبب: اتصال کند یا قطع

راه حل:
1. نمایش پیام: "Connection timeout"
2. چک کردن navigator.onLine
3. دکمه Retry
4. پیشنهاد: "Check your internet"

Code:
if (!navigator.onLine) {
  showError('No internet connection');
  return;
}
```

---

## 🎯 فلوچارت: Complete User Journey

```
┌─══════════════════════════════════════════════════════════════┐
║                    DAY 1: First Visit                          ║
└─══════════════════════════════════════════════════════════════┘

10:00 AM - کاربر وارد App می‌شود
    ↓
[App Mount]
    ↓
Check localStorage → Not Found
    ↓
Create Session → sessionId: "abc123"
    ↓
Save to localStorage
    ↓
10:01 AM - جستجو: "machine learning"
    ↓
Results: 150 موقعیت، 15 صفحه
    ↓
10:05 AM - صفحه 2، 3، 4 را می‌بیند
    ↓
10:10 AM - Tab را می‌بندد (بدون Logout)


┌─══════════════════════════════════════════════════════════════┐
║                    DAY 1: Returns (2 hours later)             ║
└─══════════════════════════════════════════════════════════════┘

12:15 PM - کاربر دوباره App را باز می‌کند
    ↓
[App Mount]
    ↓
Check localStorage → Found sessionId: "abc123"
    ↓
Validate Session → ✓ Valid (2h < 24h)
    ↓
Use Existing Session
    ↓
12:16 PM - History را می‌بیند
    ↓
جستجوی قبلی "machine learning" - page 4
    ↓
Continue from page 5 → می‌تواند ادامه دهد! ✓
    ↓
12:20 PM - جستجوی جدید: "deep learning"
    ↓
Results: 89 موقعیت
    ↓
12:25 PM - App را می‌بندد


┌─══════════════════════════════════════════════════════════════┐
║                    DAY 2: Session Expired                      ║
└─══════════════════════════════════════════════════════════════┘

10:30 AM (Next Day) - کاربر دوباره وارد می‌شود
    ↓
[App Mount]
    ↓
Check localStorage → Found sessionId: "abc123"
    ↓
Validate Session → ✗ Invalid (25h > 24h)
    ↓
Remove from localStorage
    ↓
Create New Session → sessionId: "xyz789"
    ↓
Toast: "Session renewed, please search again"
    ↓
کاربر باید جستجوی جدید کند
```

---

## 📱 فلوچارت: Multi-Tab Behavior

```
┌─══════════════════════════════════════════════════════════════┐
║            کاربر چند Tab باز می‌کند چه اتفاقی می‌افتد؟      ║
└─══════════════════════════════════════════════════════════════┘

Scenario با localStorage:
─────────────────────────────

Tab 1 (10:00 AM):
    ↓
Create Session: "session-A"
    ↓
Save to localStorage
    ↓
Search: "AI" → Results
    ↓
    │
    ├─→ Tab 2 (10:05 AM) - کاربر Tab جدید باز می‌کند:
    │       ↓
    │   Check localStorage → Found "session-A"
    │       ↓
    │   ✓ Use same Session
    │       ↓
    │   می‌تواند History ببیند
    │   می‌تواند از همان Session جستجو کند
    │       ↓
    │   Search: "ML" (در همان Session)
    │
    └─→ Tab 3 (10:10 AM):
            ↓
        Same Session "session-A"
            ↓
        All Tabs share history ✓


Scenario با sessionStorage:
────────────────────────────

Tab 1 (10:00 AM):
    ↓
Create Session: "session-A"
    ↓
Save to sessionStorage (Tab 1 only)
    ↓
    │
    ├─→ Tab 2 (10:05 AM):
    │       ↓
    │   Check sessionStorage → Not Found (مجزا از Tab 1)
    │       ↓
    │   Create New Session: "session-B"
    │       ↓
    │   هر Tab جلسه مستقل دارد
    │   History مجزا ✓
    │
    └─→ هر Tab مستقل است


┌─────────────────────────────────────────────────────────────┐
│  پیشنهاد: localStorage برای تجربه بهتر                      │
│  (تمام Tabها Session مشترک)                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚦 State Machine Diagram

```
┌─══════════════════════════════════════════════════════════════┐
║                    App State Machine                           ║
└─══════════════════════════════════════════════════════════════┘


     ┌─────────────┐
     │   INITIAL   │
     │   (Start)   │
     └──────┬──────┘
            │
            │ App Mount
            ▼
     ┌──────────────┐
     │ INITIALIZING │ ◄────── Create New Session
     │  (Loading)   │
     └──────┬───────┘
            │
            │ Session Ready
            ▼
     ┌─────────────┐
     │    READY    │ ◄─────┐
     │   (Idle)    │       │
     └──────┬──────┘       │
            │              │
            │ User Action  │
            │              │
     ┌──────┴──────┐       │
     │             │       │
     ▼             ▼       │
┌─────────┐   ┌─────────┐ │
│SEARCHING│   │LOADING  │ │
│         │   │HISTORY  │ │
└────┬────┘   └────┬────┘ │
     │             │       │
     │ Success     │       │
     ▼             ▼       │
┌──────────────────────┐  │
│   SHOWING_RESULTS    │  │
│                      │  │
└───────┬──────────────┘  │
        │                 │
        │ User Action     │
        │ (New Search/    │
        │  Load More)     │
        └─────────────────┘


     ┌──────────────┐
     │    ERROR     │
     │              │ ◄─── از هر State می‌تواند Error بیاید
     └──────┬───────┘
            │
            │ Retry/Create New Session
            │
            └───→ INITIALIZING


     ┌──────────────┐
     │  UNMOUNTING  │
     │  (Cleanup)   │
     └──────────────┘
            │
            │ App Close
            ▼
          [END]
```

---

## 🧪 فلوچارت: Testing Strategy

```
┌─══════════════════════════════════════════════════════════════┐
║               تست‌های مورد نیاز برای Frontend                 ║
└─══════════════════════════════════════════════════════════════┘

Test 1: Session Creation
────────────────────────
✓ App mount → Session created
✓ sessionId saved to localStorage
✓ isInitialized = true

Test 2: Session Validation
───────────────────────────
✓ Valid session → Used
✓ Invalid session → New created
✓ No session → New created

Test 3: Search Flow
───────────────────
✓ Submit form → API called
✓ Loading state shown
✓ Results displayed
✓ Error handled

Test 4: Pagination
──────────────────
✓ Next page loaded
✓ Results appended
✓ No duplicate results

Test 5: Error Recovery
──────────────────────
✓ Session expired → Auto-renew
✓ 503 error → Retry logic
✓ Network error → Retry button

Test 6: Multi-Tab
─────────────────
✓ Tab 2 uses same session
✓ History shared
✓ No conflicts

Test 7: Cleanup
───────────────
✓ Unmount → Session deleted (optional)
✓ localStorage cleared (optional)
```

---

## 📝 چک‌لیست کامل برای Frontend Developer

```
┌─══════════════════════════════════════════════════════════════┐
║                    Implementation Checklist                    ║
└─══════════════════════════════════════════════════════════════┘

Phase 1: Setup (30 دقیقه)
─────────────────────────
[ ] نصب dependencies (axios/fetch)
[ ] ایجاد API client (api.js)
[ ] تنظیم Environment Variables (API_URL)
[ ] تست Health Endpoint


Phase 2: Session Management (1 ساعت)
─────────────────────────────────────
[ ] ایجاد SessionContext یا State Manager
[ ] پیاده‌سازی Session Creation
[ ] پیاده‌سازی Session Validation
[ ] ذخیره در localStorage/sessionStorage
[ ] مدیریت Session Expiration
[ ] Cleanup در Unmount


Phase 3: Search Implementation (2 ساعت)
────────────────────────────────────────
[ ] ایجاد Search Form Component
[ ] پیاده‌سازی Filters (Discipline, Geography, Funding)
[ ] پیاده‌سازی Search API Call
[ ] مدیریت Loading States
[ ] نمایش Results Component
[ ] مدیریت Empty Results


Phase 4: Pagination (1 ساعت)
─────────────────────────────
[ ] پیاده‌سازی Load More Button
    یا
[ ] پیاده‌سازی Infinite Scroll
    یا
[ ] پیاده‌سازی Classic Pagination


Phase 5: Error Handling (1 ساعت)
─────────────────────────────────
[ ] مدیریت Session Expired
[ ] مدیریت 503 (No Tabs)
[ ] مدیریت Network Errors
[ ] نمایش User-friendly Messages
[ ] Retry Logic


Phase 6: UX Enhancements (1 ساعت)
──────────────────────────────────
[ ] Loading Skeletons
[ ] Toast Notifications
[ ] Search History
[ ] Debounced Search
[ ] Scroll to Top
[ ] Responsive Design


Phase 7: Testing (1 ساعت)
──────────────────────────
[ ] Unit Tests (Components)
[ ] Integration Tests (API)
[ ] E2E Tests (User Flows)
[ ] Error Scenarios
[ ] Multi-Tab Testing


Phase 8: Optimization (1 ساعت)
───────────────────────────────
[ ] Lazy Loading
[ ] Code Splitting
[ ] Caching Strategy
[ ] Performance Monitoring
[ ] SEO (if needed)


─────────────────────────────────
Total: ~8-10 ساعت برای Implementation کامل
```

---

## 🎯 خلاصه: فرایند برای هر کاربر

```
╔══════════════════════════════════════════════════════════════╗
║              SIMPLIFIED FLOW (برای توضیح به تیم)            ║
╚══════════════════════════════════════════════════════════════╝

1. 🚪 ورود به App
   └─→ بررسی Session (localStorage)
        ├─→ دارد؟ → چک اعتبار → استفاده کن
        └─→ نداره؟ → ایجاد کن → ذخیره کن

2. 🔍 جستجو
   └─→ ارسال: userId + sessionId + keywords + filters
        └─→ دریافت: searchId + results[]

3. 📄 صفحه‌های بعدی
   └─→ ارسال: searchId + sessionId + page
        └─→ دریافت: results[] (اضافه به قبلی‌ها)

4. 🕐 بعد از مدتی (5 دقیقه، 1 ساعت، ...)
   └─→ Session: ✓ زنده است (تا 24 ساعت)
        └─→ کاربر می‌تواند ادامه دهد

5. 🔄 روز بعد (بعد 24 ساعت)
   └─→ Session منقضی شده
        └─→ ایجاد Session جدید (خودکار)

6. 🚪 خروج از App
   └─→ (اختیاری) حذف Session

╔══════════════════════════════════════════════════════════════╗
║  نکته مهم: Session = شناسه کاربر (تا 24 ساعت)              ║
║             Tab = ابزار موقت جستجو (فقط چند ثانیه)         ║
╚══════════════════════════════════════════════════════════════╝
```

---

**📚 فایل‌های مرتبط:**
- [FRONTEND-GUIDE.md](./FRONTEND-GUIDE.md) - کدهای کامل
- [SESSION-MANAGEMENT-GUIDE.md](./SESSION-MANAGEMENT-GUIDE.md) - جزئیات Session
- [QUICK-REFERENCE.md](./QUICK-REFERENCE.md) - شروع سریع

**🎉 حالا فلوچارت کامل آماده است!**
