# 🎯 فلوچارت ساده برای Frontend Developer

> **نسخه ساده‌شده برای توضیح سریع**

---

## 🚀 فرایند کلی (3 قدم اصلی)

```
┌──────────────────────────────────────────────────────────┐
│                                                           │
│  1. ورود به App → چک Session                            │
│                                                           │
│  2. جستجو → نمایش نتایج                                 │
│                                                           │
│  3. خروج از App → Cleanup                               │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

---

## 📱 قدم 1: ورود به App (Session Check)

```
کاربر App را باز می‌کند
          ↓
┌──────────────────────┐
│  componentDidMount()  │
│  useEffect(() => {})  │
└──────────┬───────────┘
           │
           ▼
    ┌─────────────────┐
    │ localStorage چک؟│
    └────────┬────────┘
             │
    ┌────────┴────────┐
    │                 │
    ▼                 ▼
  دارد            نداره
    │                 │
    ▼                 │
[معتبر است?]         │
    │                 │
┌───┴───┐             │
│       │             │
▼       ▼             │
بله     خیر           │
│       │             │
│       └─────────────┘
│                     │
│                     ▼
│        ┌─────────────────────┐
│        │ ایجاد Session جدید │
│        │ POST /api/session   │
│        └─────────┬───────────┘
│                  │
│                  ▼
│         ┌────────────────┐
│         │ ذخیره Session │
│         │ در localStorage│
│         └────────┬───────┘
│                  │
└──────────────────┘
           │
           ▼
    ┌──────────────┐
    │ آماده است! ✓ │
    └──────────────┘
```

---

## 🔍 قدم 2: جستجو (Search Flow)

```
کاربر کلیدواژه وارد می‌کند
           ↓
    [دکمه Search کلیک]
           ↓
    ┌──────────────┐
    │ setLoading() │
    └──────┬───────┘
           │
           ▼
┌────────────────────────┐
│ POST /api/search       │
│ {                      │
│   userId,              │
│   sessionId,  ← از localStorage
│   keywords,            │
│   filters              │
│ }                      │
└──────────┬─────────────┘
           │
           ▼
     ┌─────────┐
     │ درخواست │
     │ ارسال   │
     └────┬────┘
          │
    ┌─────┴─────┐
    │           │
    ▼           ▼
 موفق        خطا
    │           │
    │           ▼
    │    ┌──────────────┐
    │    │ مدیریت خطا   │
    │    │ - Session؟   │
    │    │ - Network؟   │
    │    │ - 503؟       │
    │    └──────────────┘
    │
    ▼
┌─────────────────┐
│ دریافت نتایج    │
│ results[]       │
│ totalPages      │
│ searchId        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ نمایش Results   │
└─────────────────┘
         │
         ▼
   [کاربر چه کار می‌کند؟]
         │
    ┌────┼────┐
    │    │    │
    ▼    ▼    ▼
صفحه  جستجوی  خروج
بعدی   جدید
```

---

## 📄 صفحه بعدی (Pagination)

```
کاربر "Load More" می‌زند
         ↓
  ┌──────────────┐
  │ setLoading() │
  └──────┬───────┘
         │
         ▼
┌────────────────────────────┐
│ POST /api/search/:id/continue
│ {                          │
│   sessionId,               │
│   page: currentPage + 1    │
│ }                          │
└──────────┬─────────────────┘
           │
           ▼
    ┌─────────────┐
    │ نتایج جدید  │
    └──────┬──────┘
           │
           ▼
┌─────────────────────────┐
│ results = [             │
│   ...oldResults,        │
│   ...newResults         │
│ ]                       │
└──────────┬──────────────┘
           │
           ▼
    ┌──────────────┐
    │ نمایش همه    │
    └──────────────┘
```

---

## 🗂️ ساختار State

```javascript
// State که باید نگهداری کنید:

const [state, setState] = useState({
  
  // Session
  sessionId: null,        // "session-abc-123"
  isInitialized: false,   // true/false
  
  // Search
  currentSearch: {
    searchId: null,       // "search-xyz-789"
    keywords: "",         // "machine learning"
    filters: {},          // { discipline: "..." }
    results: [],          // [{ title, url, ... }]
    currentPage: 1,       // 1, 2, 3, ...
    totalPages: 10        // 10
  },
  
  // UI
  loading: false,         // true/false
  error: null,            // null or "error message"
  
});
```

---

## 📦 localStorage Structure

```javascript
// چه چیزی در localStorage ذخیره می‌کنیم:

localStorage.setItem('phdSearchSession', JSON.stringify({
  sessionId: "session-abc-123",
  userId: "user-123",
  createdAt: 1696512000000,      // timestamp
  expiresAt: 1696598400000       // timestamp (24h بعد)
}));

// خواندن:
const stored = localStorage.getItem('phdSearchSession');
const sessionData = JSON.parse(stored);

// چک expiration:
if (Date.now() < sessionData.expiresAt) {
  // معتبر است ✓
} else {
  // منقضی شده ✗
}
```

---

## ⚠️ خطاهای رایج

```
┌──────────────────────────────────────────────────────┐
│  خطا: "Session not found"                           │
├──────────────────────────────────────────────────────┤
│  سبب: Session منقضی شده (بیشتر از 24 ساعت)         │
├──────────────────────────────────────────────────────┤
│  راه حل:                                             │
│  1. حذف از localStorage                             │
│  2. ایجاد Session جدید                              │
│  3. پیام به کاربر: "Session renewed"               │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│  خطا: 503 Service Unavailable                       │
├──────────────────────────────────────────────────────┤
│  سبب: همه Browser Tab ها پر هستند                  │
├──────────────────────────────────────────────────────┤
│  راه حل:                                             │
│  1. نمایش: "Server is busy..."                     │
│  2. Retry بعد 5 ثانیه                               │
│  3. حداکثر 3 بار تلاش                               │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│  خطا: Network Timeout                               │
├──────────────────────────────────────────────────────┤
│  سبب: اینترنت قطع یا خیلی کند                       │
├──────────────────────────────────────────────────────┤
│  راه حل:                                             │
│  1. چک navigator.onLine                             │
│  2. نمایش: "Check your internet"                   │
│  3. دکمه Retry                                       │
└──────────────────────────────────────────────────────┘
```

---

## 🎯 چک‌لیست سریع

```
برای هر کاربر:

✅ گام 1: چک Session در localStorage
   ├─ دارد؟ → چک expiration
   │   ├─ معتبر؟ → استفاده کن
   │   └─ منقضی؟ → ایجاد جدید
   └─ نداره؟ → ایجاد جدید

✅ گام 2: ذخیره sessionId
   ├─ در State
   └─ در localStorage

✅ گام 3: برای جستجو
   ├─ ارسال: userId + sessionId + keywords
   └─ دریافت: searchId + results

✅ گام 4: برای صفحه بعدی
   ├─ ارسال: searchId + sessionId + page
   └─ دریافت: results (اضافه کن)

✅ گام 5: مدیریت خطا
   ├─ Session expired? → ایجاد جدید
   ├─ 503? → Retry
   └─ Network? → نمایش پیام

✅ گام 6: Cleanup (اختیاری)
   └─ حذف Session در unmount
```

---

## 🔄 Timeline مثال

```
┌─────────────────────────────────────────────────────────┐
│  10:00 AM - کاربر وارد App می‌شود                       │
│            → چک localStorage: ندارد                     │
│            → ایجاد Session: "session-A"                 │
│            → ذخیره در localStorage                      │
├─────────────────────────────────────────────────────────┤
│  10:05 AM - جستجو: "machine learning"                  │
│            → ارسال با sessionId: "session-A"            │
│            → دریافت: 150 نتیجه، 15 صفحه                │
├─────────────────────────────────────────────────────────┤
│  10:10 AM - صفحه 2، 3 را می‌بیند                       │
│            → همان Session: "session-A"                  │
├─────────────────────────────────────────────────────────┤
│  10:15 AM - App را می‌بندد                             │
│            → Session در localStorage باقی می‌ماند       │
├─────────────────────────────────────────────────────────┤
│  12:00 PM - دوباره App را باز می‌کند (2 ساعت بعد)     │
│            → چک localStorage: دارد "session-A"         │
│            → چک expiration: معتبر است ✓                │
│            → استفاده از همان Session                    │
│            → می‌تواند از صفحه 3 ادامه دهد ✓            │
├─────────────────────────────────────────────────────────┤
│  10:00 AM (روز بعد) - دوباره وارد می‌شود              │
│            → چک localStorage: دارد "session-A"         │
│            → چک expiration: منقضی شده ✗ (25 ساعت)     │
│            → حذف Session قدیمی                          │
│            → ایجاد Session جدید: "session-B"           │
└─────────────────────────────────────────────────────────┘
```

---

## 💡 نکات مهم

### ✅ انجام دهید:
- ✓ Session را در `componentDidMount` / `useEffect` چک کنید
- ✓ همیشه expiration را بررسی کنید
- ✓ sessionId را با هر request ارسال کنید
- ✓ خطاهای Session expired را مدیریت کنید
- ✓ Loading state برای تمام API calls

### ❌ انجام ندهید:
- ✗ برای هر جستجو Session جدید نسازید
- ✗ sessionId را در URL نگذارید (امنیتی نیست)
- ✗ بدون چک expiration از Session استفاده نکنید
- ✗ localStorage را بدون try-catch نخوانید
- ✗ خطاها را نادیده نگیرید

---

## 🎉 خلاصه کلی

```
┌──────────────────────────────────────────────────┐
│                                                   │
│  1 Session = 1 کاربر = 24 ساعت                  │
│                                                   │
│  Session در localStorage ذخیره می‌شود            │
│                                                   │
│  همیشه Session را اول چک کنید                   │
│                                                   │
│  Session منقضی شد؟ → ایجاد جدید (خودکار)        │
│                                                   │
└──────────────────────────────────────────────────┘
```

---

**📁 فایل‌های مرتبط:**
- [FRONTEND-GUIDE.md](./FRONTEND-GUIDE.md) - کدهای کامل React
- [FRONTEND-FLOWCHART.md](./FRONTEND-FLOWCHART.md) - فلوچارت‌های دقیق
- [SESSION-MANAGEMENT-GUIDE.md](./SESSION-MANAGEMENT-GUIDE.md) - جزئیات Session

---

**🎯 این فایل برای: توضیح سریع فرایند به تیم Frontend**
